# Для компиляции используйте команду:
``` bash
gcc optimalSorting.c quickSort.s -o program
```
# Измененная команда под чтобы подключать заголовочник:
``` bash
gcc optimalSorting.c quickSort.c -o program
```
# Запуск:
```bash
./program 
```